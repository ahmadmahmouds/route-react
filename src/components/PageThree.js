import React from 'react'
import {Link} from '@reach/router';
 const PageThree = (props) => {
    return (
        <div>
            <h1>this is a string {props.hello}</h1>
            
        </div>
    )
}
export default PageThree;
